#include "../include/cub3d.h"

void	raycaster(t_game *g)
{
	int	i;

	i = -1;
	//something to clear ?
	while (++i < g->map_width * 64)
	{
//		get_ray_posdir(g, i);
//		set_step(g);
//		mathing(g);
//		get_draw_len(g, i);
//		bob_ross(g, i);
//		i++;
	}
}